async function testNumber() {
    const num = await new Promise((resolve, reject) => {
        let testNumber = Math.floor(Math.random() * 10) + 1
        resolve(num)
    }) // สุ่มเลข
    console.log(result)
}

testNumber()
